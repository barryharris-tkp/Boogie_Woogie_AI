# Optional OpenAI cover-art key. Music and lyrics do not need this.
# Prefer Studio settings -> Add API key -> Remember this key on this PC.
# Never commit .env.local or paste a real key into this example.
# An explicit blank in .env.local disables an inherited environment key.
OPENAI_API_KEY=
